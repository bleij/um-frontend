
  # High-fidelity mobile app design

  This is a code bundle for High-fidelity mobile app design. The original project is available at https://www.figma.com/design/grpMs9T0ws4B3pd7V4incD/High-fidelity-mobile-app-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  